#include <iostream>

int main()
{
    // int x = 10;
    // int y = x++; // y = 10; x = 11;
    // int z = ++x; // z = 11; x = 11;

    // ()
    // * and /
    // = and -

    // int x = 1 + 3 * 2;
    // int x = (1 + 3) * 2;

    double x = 10;
    int y = 5;

    double z = (x + 10) / (3 * y);

    std::cout << z;
    return 0;
}